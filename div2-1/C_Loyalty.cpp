#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main() {
    int n;
    cin >> n;
    while (n--)
    {
        int t, x;
        int r = 0;
        int s = 0;
        int b = 0;
        cin >> t, x;
        vector<int> v(t);
        for (size_t i = 0; i < t; i++)
        {
            cin >> v[i];
        }

        
    }
    
}