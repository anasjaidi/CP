#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main() {
    int n;

    cin >> n;

    while (n--)
    {
        int t;
        cin >> t;
        if (t == 1) {
            cout << "yes";
            continue;
        }
        vector<string>s(t);
        for (size_t i = 0; i < t; i++)
        {
            cin >> s[i];
        }
        int a = 0, b = 0;
        for (size_t i = 0; i < t; i++)
        {
            if (s[i][i])
        }
        
    }
    
}